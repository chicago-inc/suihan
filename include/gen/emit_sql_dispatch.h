/*
 * Generated by suhc (the suihan compiler) from ordbok/compiler/compiler_emit_sql_dispatch.szh
 * DO NOT EDIT — regenerate from the .szh source.
 *
 * This is a self-contained C11 header.
 * Include it in exactly one translation unit, or use
 * static inline functions (already the default).
 */

#ifndef SUHC_GEN_COMPILER_EMIT_SQL_DISPATCH_SZH
#define SUHC_GEN_COMPILER_EMIT_SQL_DISPATCH_SZH

#include <math.h>
#include <stddef.h>
#include <stdbool.h>
#include <string.h>

/* import compiler_decl_types */
/* dispatch: emit_sql_dispatch (decl → handler) */
/* Generated by suhc — do not edit by hand. */
/* Include after all handler definitions.    */

static inline void emit_sql_dispatch(FILE *out, Decl *d) {
    switch (d->type) {
    case DECL_DIMENSION: emit_dimension_sql(out, d); break;
    case DECL_DEPENDENCY: emit_dependency_sql_d(out, d); break;
    case DECL_PROJECTION: emit_projection_sql(out, d); break;
    case DECL_MORPHISM: emit_morphism_sql(out, d); break;
    case DECL_TRAVERSAL: emit_traversal_sql(out, d); break;
    case DECL_MEIHUA: emit_meihua_sql(out, d); break;
    case DECL_ZHULIN: emit_meihua_sql(out, d); break;
    case DECL_SONGQIAO: emit_songqiao_sql(out, d); break;
    case DECL_UNIT: emit_unit_sql_comment(out, d); break;
    case DECL_MAGNITUDE: emit_unit_sql_comment(out, d); break;
    case DECL_VECTOR: emit_unit_sql_comment(out, d); break;
    case DECL_IMPORT: emit_import_sql_comment(out, d); break;
    case DECL_ZERO: break;
    case DECL_CONTAINMENT: break;
    case DECL_INCOMMENSURABLE: break;
    case DECL_COMMENSURABLE: break;
    case DECL_PERPENDICULAR: break;
    case DECL_KINDED_VALUE: break;
    case DECL_JOURNEY: break;
    case DECL_PROGRAM: break;
    }
}

#endif /* SUHC_GEN_COMPILER_EMIT_SQL_DISPATCH_SZH */
