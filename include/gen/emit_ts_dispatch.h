/*
 * Generated by suhc (the suihan compiler) from ordbok/compiler/compiler_emit_ts_dispatch.szh
 * DO NOT EDIT — regenerate from the .szh source.
 *
 * This is a self-contained C11 header.
 * Include it in exactly one translation unit, or use
 * static inline functions (already the default).
 */

#ifndef SUHC_GEN_COMPILER_EMIT_TS_DISPATCH_SZH
#define SUHC_GEN_COMPILER_EMIT_TS_DISPATCH_SZH

#include <math.h>
#include <stddef.h>
#include <stdbool.h>
#include <string.h>

/* import compiler_decl_types */
/* dispatch: emit_ts_dispatch (decl → handler) */
/* Generated by suhc — do not edit by hand. */
/* Include after all handler definitions.    */

static inline void emit_ts_dispatch(FILE *out, Decl *d) {
    switch (d->type) {
    case DECL_UNIT: emit_unit(out, d); break;
    case DECL_MAGNITUDE: emit_unit(out, d); break;
    case DECL_VECTOR: emit_unit(out, d); break;
    case DECL_KINDED_VALUE: emit_kinded_value_ts(out, d); break;
    case DECL_DIMENSION: emit_dimension(out, d); break;
    case DECL_PERPENDICULAR: emit_perpendicular(out, d); break;
    case DECL_INCOMMENSURABLE: emit_relation(out, d); break;
    case DECL_COMMENSURABLE: emit_relation(out, d); break;
    case DECL_DEPENDENCY: emit_dependency_ts(out, d); break;
    case DECL_MORPHISM: emit_morphism(out, d); break;
    case DECL_PROJECTION: emit_projection_ts(out, d); break;
    case DECL_TRAVERSAL: emit_traversal(out, d); break;
    case DECL_MEIHUA: emit_meihua(out, d); break;
    case DECL_SONGQIAO: emit_songqiao(out, d); break;
    case DECL_ZHULIN: emit_meihua(out, d); break;
    case DECL_IMPORT: break;
    case DECL_ZERO: break;
    case DECL_CONTAINMENT: break;
    case DECL_JOURNEY: break;
    case DECL_PROGRAM: break;
    }
}

#endif /* SUHC_GEN_COMPILER_EMIT_TS_DISPATCH_SZH */
