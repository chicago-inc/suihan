/*
 * Generated by suhc (the suihan compiler) from ordbok/compiler/compiler_math_fns.szh
 * DO NOT EDIT — regenerate from the .szh source.
 *
 * This is a self-contained C11 header.
 * Include it in exactly one translation unit, or use
 * static inline functions (already the default).
 */

#ifndef SUHC_GEN_COMPILER_MATH_FNS_SZH
#define SUHC_GEN_COMPILER_MATH_FNS_SZH

#include <math.h>
#include <stddef.h>
#include <stdbool.h>
#include <string.h>

/* dimension math_fn */
typedef enum {
    MATH_FN_SQRT,
    MATH_FN_ABS,
    MATH_FN_FLOOR,
    MATH_FN_CEIL,
    MATH_FN_ROUND,
    MATH_FN_SIN,
    MATH_FN_COS,
    MATH_FN_TAN,
    MATH_FN_ACOS,
    MATH_FN_ASIN,
    MATH_FN_ATAN,
    MATH_FN_ATAN2,
    MATH_FN_POW,
    MATH_FN_LOG,
    MATH_FN_LOG2,
    MATH_FN_MIN,
    MATH_FN_MAX
} math_fn_t;

static inline const char *math_fn_to_str(math_fn_t val) {
    switch (val) {
    case MATH_FN_SQRT: return "sqrt";
    case MATH_FN_ABS: return "abs";
    case MATH_FN_FLOOR: return "floor";
    case MATH_FN_CEIL: return "ceil";
    case MATH_FN_ROUND: return "round";
    case MATH_FN_SIN: return "sin";
    case MATH_FN_COS: return "cos";
    case MATH_FN_TAN: return "tan";
    case MATH_FN_ACOS: return "acos";
    case MATH_FN_ASIN: return "asin";
    case MATH_FN_ATAN: return "atan";
    case MATH_FN_ATAN2: return "atan2";
    case MATH_FN_POW: return "pow";
    case MATH_FN_LOG: return "log";
    case MATH_FN_LOG2: return "log2";
    case MATH_FN_MIN: return "min";
    case MATH_FN_MAX: return "max";
    }
    return "unknown";
}

static inline math_fn_t math_fn_from_name(const char *name) {
    if (strcmp(name, "sqrt") == 0) return MATH_FN_SQRT;
    if (strcmp(name, "abs") == 0) return MATH_FN_ABS;
    if (strcmp(name, "floor") == 0) return MATH_FN_FLOOR;
    if (strcmp(name, "ceil") == 0) return MATH_FN_CEIL;
    if (strcmp(name, "round") == 0) return MATH_FN_ROUND;
    if (strcmp(name, "sin") == 0) return MATH_FN_SIN;
    if (strcmp(name, "cos") == 0) return MATH_FN_COS;
    if (strcmp(name, "tan") == 0) return MATH_FN_TAN;
    if (strcmp(name, "acos") == 0) return MATH_FN_ACOS;
    if (strcmp(name, "asin") == 0) return MATH_FN_ASIN;
    if (strcmp(name, "atan") == 0) return MATH_FN_ATAN;
    if (strcmp(name, "atan2") == 0) return MATH_FN_ATAN2;
    if (strcmp(name, "pow") == 0) return MATH_FN_POW;
    if (strcmp(name, "log") == 0) return MATH_FN_LOG;
    if (strcmp(name, "log2") == 0) return MATH_FN_LOG2;
    if (strcmp(name, "min") == 0) return MATH_FN_MIN;
    if (strcmp(name, "max") == 0) return MATH_FN_MAX;
    return (math_fn_t)-1;
}

#endif /* SUHC_GEN_COMPILER_MATH_FNS_SZH */
