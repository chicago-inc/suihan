/*
 * Generated by suhc (the suihan compiler) from ordbok/compiler/compiler_emit_c_dispatch.szh
 * DO NOT EDIT — regenerate from the .szh source.
 *
 * This is a self-contained C11 header.
 * Include it in exactly one translation unit, or use
 * static inline functions (already the default).
 */

#ifndef SUHC_GEN_COMPILER_EMIT_C_DISPATCH_SZH
#define SUHC_GEN_COMPILER_EMIT_C_DISPATCH_SZH

#include <math.h>
#include <stddef.h>
#include <stdbool.h>
#include <string.h>

/* import compiler_decl_types */
/* dispatch: emit_c_dispatch (decl → handler) */
/* Generated by suhc — do not edit by hand. */
/* Include after all handler definitions.    */

static inline void emit_c_dispatch(FILE *out, Decl *d) {
    switch (d->type) {
    case DECL_DIMENSION: emit_c_dimension(out, d); break;
    case DECL_DEPENDENCY: emit_c_dependency(out, d); break;
    case DECL_PROJECTION: emit_c_projection(out, d); break;
    case DECL_MORPHISM: emit_c_morphism(out, d); break;
    case DECL_TRAVERSAL: emit_c_traversal(out, d); break;
    case DECL_MEIHUA: emit_c_meihua(out, d); break;
    case DECL_ZHULIN: emit_c_zhulin(out, d); break;
    case DECL_SONGQIAO: emit_c_songqiao(out, d); break;
    case DECL_UNIT: emit_c_unit(out, d); break;
    case DECL_MAGNITUDE: emit_c_magnitude(out, d); break;
    case DECL_VECTOR: emit_c_vector(out, d); break;
    case DECL_KINDED_VALUE: emit_c_kinded_value(out, d); break;
    case DECL_INCOMMENSURABLE: break;
    case DECL_COMMENSURABLE: break;
    case DECL_PERPENDICULAR: break;
    case DECL_IMPORT: emit_c_import_comment(out, d); break;
    case DECL_ZERO: break;
    case DECL_CONTAINMENT: break;
    case DECL_JOURNEY: break;
    case DECL_PROGRAM: break;
    case DECL_GRAPHICS_RULE: break;
    }
}

#endif /* SUHC_GEN_COMPILER_EMIT_C_DISPATCH_SZH */
