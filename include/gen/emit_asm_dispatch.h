/*
 * Generated by suhc (the suihan compiler) from ordbok/compiler/compiler_emit_asm_dispatch.szh
 * DO NOT EDIT — regenerate from the .szh source.
 *
 * This is a self-contained C11 header.
 * Include it in exactly one translation unit, or use
 * static inline functions (already the default).
 */

#ifndef SUHC_GEN_COMPILER_EMIT_ASM_DISPATCH_SZH
#define SUHC_GEN_COMPILER_EMIT_ASM_DISPATCH_SZH

#include <math.h>
#include <stddef.h>
#include <stdbool.h>
#include <string.h>

/* import compiler_decl_types */
/* dispatch: emit_asm_dispatch (decl → handler) */
/* Generated by suhc — do not edit by hand. */
/* Include after all handler definitions.    */

static inline void emit_asm_dispatch(FILE *out, Decl *d) {
    switch (d->type) {
    case DECL_DIMENSION: emit_asm_dimension(out, d); break;
    case DECL_DEPENDENCY: break;
    case DECL_PROJECTION: emit_asm_projection(out, d); break;
    case DECL_MORPHISM: break;
    case DECL_TRAVERSAL: break;
    case DECL_MEIHUA: emit_asm_meihua(out, d); break;
    case DECL_ZHULIN: emit_asm_zhulin(out, d); break;
    case DECL_SONGQIAO: emit_asm_songqiao(out, d); break;
    case DECL_UNIT: break;
    case DECL_MAGNITUDE: break;
    case DECL_VECTOR: break;
    case DECL_KINDED_VALUE: emit_asm_kinded_value(out, d); break;
    case DECL_INCOMMENSURABLE: break;
    case DECL_COMMENSURABLE: break;
    case DECL_PERPENDICULAR: break;
    case DECL_IMPORT: break;
    case DECL_ZERO: break;
    case DECL_CONTAINMENT: break;
    }
}

#endif /* SUHC_GEN_COMPILER_EMIT_ASM_DISPATCH_SZH */
