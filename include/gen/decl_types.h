/*
 * Generated by suhc (the suihan compiler) from ordbok/compiler/compiler_decl_types.szh
 * DO NOT EDIT — regenerate from the .szh source.
 *
 * This is a self-contained C11 header.
 * Include it in exactly one translation unit, or use
 * static inline functions (already the default).
 */

#ifndef SUHC_GEN_COMPILER_DECL_TYPES_SZH
#define SUHC_GEN_COMPILER_DECL_TYPES_SZH

#include <math.h>
#include <stddef.h>
#include <stdbool.h>
#include <string.h>

/* dimension decl */
typedef enum {
    DECL_UNIT,
    DECL_ZERO,
    DECL_MAGNITUDE,
    DECL_VECTOR,
    DECL_DIMENSION,
    DECL_DEPENDENCY,
    DECL_CONTAINMENT,
    DECL_MORPHISM,
    DECL_PROJECTION,
    DECL_TRAVERSAL,
    DECL_INCOMMENSURABLE,
    DECL_COMMENSURABLE,
    DECL_PERPENDICULAR,
    DECL_KINDED_VALUE,
    DECL_MEIHUA,
    DECL_ZHULIN,
    DECL_SONGQIAO,
    DECL_IMPORT
} decl_t;

static inline const char *decl_to_str(decl_t val) {
    switch (val) {
    case DECL_UNIT: return "unit";
    case DECL_ZERO: return "zero";
    case DECL_MAGNITUDE: return "magnitude";
    case DECL_VECTOR: return "vector";
    case DECL_DIMENSION: return "dimension";
    case DECL_DEPENDENCY: return "dependency";
    case DECL_CONTAINMENT: return "containment";
    case DECL_MORPHISM: return "morphism";
    case DECL_PROJECTION: return "projection";
    case DECL_TRAVERSAL: return "traversal";
    case DECL_INCOMMENSURABLE: return "incommensurable";
    case DECL_COMMENSURABLE: return "commensurable";
    case DECL_PERPENDICULAR: return "perpendicular";
    case DECL_KINDED_VALUE: return "kinded_value";
    case DECL_MEIHUA: return "meihua";
    case DECL_ZHULIN: return "zhulin";
    case DECL_SONGQIAO: return "songqiao";
    case DECL_IMPORT: return "import";
    }
    return "unknown";
}

static inline decl_t decl_from_name(const char *name) {
    if (strcmp(name, "unit") == 0) return DECL_UNIT;
    if (strcmp(name, "zero") == 0) return DECL_ZERO;
    if (strcmp(name, "magnitude") == 0) return DECL_MAGNITUDE;
    if (strcmp(name, "vector") == 0) return DECL_VECTOR;
    if (strcmp(name, "dimension") == 0) return DECL_DIMENSION;
    if (strcmp(name, "dependency") == 0) return DECL_DEPENDENCY;
    if (strcmp(name, "containment") == 0) return DECL_CONTAINMENT;
    if (strcmp(name, "morphism") == 0) return DECL_MORPHISM;
    if (strcmp(name, "projection") == 0) return DECL_PROJECTION;
    if (strcmp(name, "traversal") == 0) return DECL_TRAVERSAL;
    if (strcmp(name, "incommensurable") == 0) return DECL_INCOMMENSURABLE;
    if (strcmp(name, "commensurable") == 0) return DECL_COMMENSURABLE;
    if (strcmp(name, "perpendicular") == 0) return DECL_PERPENDICULAR;
    if (strcmp(name, "kinded_value") == 0) return DECL_KINDED_VALUE;
    if (strcmp(name, "meihua") == 0) return DECL_MEIHUA;
    if (strcmp(name, "zhulin") == 0) return DECL_ZHULIN;
    if (strcmp(name, "songqiao") == 0) return DECL_SONGQIAO;
    if (strcmp(name, "import") == 0) return DECL_IMPORT;
    return (decl_t)-1;
}

#endif /* SUHC_GEN_COMPILER_DECL_TYPES_SZH */
