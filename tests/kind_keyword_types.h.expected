/*
 * Generated by suhc (the suihan compiler) from ./tests/kind_keyword_types.szh
 * DO NOT EDIT — regenerate from the .szh source.
 *
 * This is a self-contained C11 header.
 * Include it in exactly one translation unit, or use
 * static inline functions (already the default).
 */

#ifndef SUHC_GEN_KIND_KEYWORD_TYPES_SZH
#define SUHC_GEN_KIND_KEYWORD_TYPES_SZH

#include <math.h>
#include <stddef.h>
#include <stdbool.h>
#include <string.h>

/* unit meter */
typedef double meter_t;

/* magnitude distance */
typedef double distance_t;

/* vector velocity */
typedef double velocity_t;

/* dimension directions */
typedef enum {
    DIRECTIONS_NORTH,
    DIRECTIONS_SOUTH,
    DIRECTIONS_EAST,
    DIRECTIONS_WEST
} directions_t;

static inline const char *directions_to_str(directions_t val) {
    switch (val) {
    case DIRECTIONS_NORTH: return "north";
    case DIRECTIONS_SOUTH: return "south";
    case DIRECTIONS_EAST: return "east";
    case DIRECTIONS_WEST: return "west";
    }
    return "unknown";
}

static inline directions_t directions_from_name(const char *name) {
    if (strcmp(name, "north") == 0) return DIRECTIONS_NORTH;
    if (strcmp(name, "south") == 0) return DIRECTIONS_SOUTH;
    if (strcmp(name, "east") == 0) return DIRECTIONS_EAST;
    if (strcmp(name, "west") == 0) return DIRECTIONS_WEST;
    return (directions_t)-1;
}

/* meihua typed_unit — pure computation */
static inline double typed_unit(double a) {
    return (a + 1);
}

/* meihua typed_magnitude — pure computation */
static inline double typed_magnitude(double a, double b) {
    return (a + b);
}

/* meihua typed_vector — pure computation */
static inline double typed_vector(double a) {
    return (a * 2);
}

/* meihua typed_dimension — pure computation */
static inline double typed_dimension(double a) {
    return (a + 1);
}

#endif /* SUHC_GEN_KIND_KEYWORD_TYPES_SZH */
