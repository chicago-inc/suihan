meihua safe_divide(a, b) :
  if b == 0 then 0 else a / b

meihua clamp_v2(value, low, high) :
  if value < low then low else if value > high then high else value

meihua abs_value(x) :
  if x < 0 then -x else x

meihua sign(n) :
  if n > 0 then 1 else if n < 0 then -1 else 0
