/*
 * Generated by suhc (the suihan compiler) from ./tests/songqiao_test.szh
 * DO NOT EDIT — regenerate from the .szh source.
 *
 * This is a self-contained C11 header.
 * Include it in exactly one translation unit, or use
 * static inline functions (already the default).
 */

#ifndef SUHC_GEN_SONGQIAO_TEST_SZH
#define SUHC_GEN_SONGQIAO_TEST_SZH

#include <math.h>
#include <stddef.h>
#include <stdbool.h>
#include <string.h>

/* songqiao tool_readiness — runtime configuration */
static inline const char *songqiao_tool_readiness(const char *tool_state) {
    return ((tool_state == "ready")) ? 1 : 0;
}

/* songqiao cognitive_budget — runtime configuration */
static inline const char *songqiao_cognitive_budget(const char *mechanism) {
    return (strcmp(mechanism, "genevieve") == 0 ? 0 : (strcmp(mechanism, "eleanor") == 0 ? 1 : (strcmp(mechanism, "emily") == 0 ? 3 : 99)));
}

#endif /* SUHC_GEN_SONGQIAO_TEST_SZH */
