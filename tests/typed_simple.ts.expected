/**
 * Generated by suhc (the suihan compiler) from ./tests/typed_simple.szh
 * DO NOT EDIT — regenerate from the .szh source.
 *
 * Kind system guarantees:
 *   ξ values are immutable (const)
 *   ζ values are computed (functions, never stored)
 *   Perpendicular dimensions use branded types
 */

/** unit meter */
export interface Meter {
  readonly id: number;
}

/** unit kilogram */
export interface Kilogram {
  readonly id: number;
}

/** unit distance */
export interface Distance {
  readonly id: number;
}

/** unit weight */
export interface Weight {
  readonly id: number;
}

// incommensurable distance, weight
// These types are completely distinct — no lossless conversion.

/** meihua add_distance — pure computation (no side effects) */
export function addDistance(a: number, b: number): number {
  return (a + b);
}

/** meihua scale — pure computation (no side effects) */
export function scale(val: number, factor: number): number {
  return (val * factor);
}

