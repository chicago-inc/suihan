/**
 * Generated by suhc (the suihan compiler) from ./tests/cross_product_test.szh
 * DO NOT EDIT — regenerate from the .szh source.
 *
 * Kind system guarantees:
 *   ξ values are immutable (const)
 *   ζ values are computed (functions, never stored)
 *   Perpendicular dimensions use branded types
 */

/** dimension roles */
type RolesValue = string | null;
export type Roles = RolesValue & { readonly __brand: 'roles' };

/** dimension actions */
type ActionsValue = string | null;
export type Actions = ActionsValue & { readonly __brand: 'actions' };

/**
 * projection permission_matrix
 * invariant × context → appearance (D12)
 * invariant (ξ): role
 * context (ζ):   action
 * yields (ω):    allowed
 */
export type Role = 'owner' | 'admin' | 'member';
export type Action = 'edit' | 'delete' | 'cancel';
export function resolvePermissionMatrix(
  role: Role,
  action: Action
): string {
  if (role === 'owner' && action === 'edit') return 'true';
  if (role === 'owner' && action === 'delete') return 'true';
  if (role === 'admin' && action === 'edit') return 'true';
  if (role === 'admin' && action === 'delete') return 'false';
  if (role === 'member') return 'false';

  return 'denied';
}

