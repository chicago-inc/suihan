/**
 * Generated by suhc (the suihan compiler) from ./tests/typed_emission.szh
 * DO NOT EDIT — regenerate from the .szh source.
 *
 * Kind system guarantees:
 *   ξ values are immutable (const)
 *   ζ values are computed (functions, never stored)
 *   Perpendicular dimensions use branded types
 */

/** dimension colors */
type ColorsValue = string | null;
export type Colors = ColorsValue & { readonly __brand: 'colors' };

/** dimension sizes */
type SizesValue = string | null;
export type Sizes = SizesValue & { readonly __brand: 'sizes' };

/**
 * projection color_label
 * invariant × context → appearance (D12)
 * invariant (ξ): color
 * context (ζ):   size
 * yields (ω):    label
 */
export type Color = 'red' | 'green' | 'blue';
export type Size = 'small' | 'medium' | 'large';
export function resolveColorLabel(
  color: Color,
  size: Size
): string {
  if (color === 'red' && size === 'small') return 'Small Red';
  if (color === 'red' && size === 'medium') return 'Medium Red';
  if (color === 'red' && size === 'large') return 'Large Red';
  if (color === 'green' && size === 'small') return 'Small Green';
  if (color === 'green' && size === 'medium') return 'Medium Green';
  if (color === 'green' && size === 'large') return 'Large Green';
  if (color === 'blue' && size === 'small') return 'Small Blue';
  if (color === 'blue' && size === 'medium') return 'Medium Blue';
  if (color === 'blue' && size === 'large') return 'Large Blue';

  return 'Unknown';
}

