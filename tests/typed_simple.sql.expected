-- Generated by suhc (the suihan compiler) from ./tests/typed_simple.szh
-- DO NOT EDIT — regenerate from the .szh source.
--
-- Dependencies are emitted as CREATE TABLE statements.
-- Review and adjust column types before running.

-- unit meter (structural — no SQL output)

-- unit kilogram (structural — no SQL output)

-- unit distance (structural — no SQL output)

-- unit weight (structural — no SQL output)

-- meihua add_distance — pure computation
CREATE OR REPLACE FUNCTION add_distance(
  p_a double precision,
  p_b double precision
) RETURNS double precision
  LANGUAGE sql
  IMMUTABLE
AS $$
  SELECT (p_a + p_b)
$$;

-- meihua scale — pure computation
CREATE OR REPLACE FUNCTION scale(
  p_val double precision,
  p_factor double precision
) RETURNS double precision
  LANGUAGE sql
  IMMUTABLE
AS $$
  SELECT (p_val * p_factor)
$$;

