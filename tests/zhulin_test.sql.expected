-- Generated by suhc (the suihan compiler) from ./tests/zhulin_test.szh
-- DO NOT EDIT — regenerate from the .szh source.
--
-- Dependencies are emitted as CREATE TABLE statements.
-- Review and adjust column types before running.

-- meihua resolve_path — pure computation
CREATE OR REPLACE FUNCTION resolve_path(
  p_headedness double precision
) RETURNS double precision
  LANGUAGE sql
  IMMUTABLE
AS $$
  SELECT CASE p_headedness WHEN 'unheaded' THEN 'conjunction' WHEN 'headed' THEN 'filter' ELSE 'expand' END
$$;

-- meihua gate_check — pure computation
CREATE OR REPLACE FUNCTION gate_check(
  p_role_count double precision,
  p_max_roles double precision
) RETURNS double precision
  LANGUAGE sql
  IMMUTABLE
AS $$
  SELECT CASE WHEN (p_role_count > p_max_roles) THEN 0 ELSE 1 END
$$;

