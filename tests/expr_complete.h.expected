/*
 * Generated by suhc (the suihan compiler) from ./tests/expr_complete.szh
 * DO NOT EDIT — regenerate from the .szh source.
 *
 * This is a self-contained C11 header.
 * Include it in exactly one translation unit, or use
 * static inline functions (already the default).
 */

#ifndef SUHC_GEN_EXPR_COMPLETE_SZH
#define SUHC_GEN_EXPR_COMPLETE_SZH

#include <math.h>
#include <stddef.h>
#include <stdbool.h>
#include <string.h>

/* meihua safe_name — pure computation */
static inline double safe_name(double name, double fallback) {
    return (name != 0 ? name : fallback);
}

/* meihua classify_tier — pure computation */
static inline double classify_tier(double score) {
    return (score == 90 ? "gold" : (score == 70 ? "silver" : "bronze"));
}

/* meihua make_label — pure computation */
static inline double make_label(double prefix, double suffix) {
    return (prefix != 0 ? prefix : "default");
}

/* meihua build_list — pure computation */
static inline double build_list(double a, double b, double c) {
    return 0 /* list */;
}

#endif /* SUHC_GEN_EXPR_COMPLETE_SZH */
