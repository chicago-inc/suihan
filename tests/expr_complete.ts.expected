/**
 * Generated by suhc (the suihan compiler) from ./tests/expr_complete.szh
 * DO NOT EDIT — regenerate from the .szh source.
 *
 * Kind system guarantees:
 *   ξ values are immutable (const)
 *   ζ values are computed (functions, never stored)
 *   Perpendicular dimensions use branded types
 */

/** meihua safe_name — pure computation (no side effects) */
export function safeName(name: number, fallback: number): number {
  return (name ?? fallback);
}

/** meihua classify_tier — pure computation (no side effects) */
export function classifyTier(score: number): number {
  return (score === 90) ? 'gold' : (score === 70) ? 'silver' : 'bronze';
}

/** meihua make_label — pure computation (no side effects) */
export function makeLabel(prefix: number, suffix: number): number {
  return (prefix ?? 'default');
}

/** meihua build_list — pure computation (no side effects) */
export function buildList(a: number, b: number, c: number): number {
  return [a, b, c];
}


/* Kind manifest — 256-bit perpendicular encoding metadata */
export const __kindManifest = {
  'safe_name': 'xi',
  'classify_tier': 'xi',
  'make_label': 'xi',
  'build_list': 'xi'
} as const;

export type KindOf<K extends keyof typeof __kindManifest> = typeof __kindManifest[K];
