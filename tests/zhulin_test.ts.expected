/**
 * Generated by suhc (the suihan compiler) from ./tests/zhulin_test.szh
 * DO NOT EDIT — regenerate from the .szh source.
 *
 * Kind system guarantees:
 *   ξ values are immutable (const)
 *   ζ values are computed (functions, never stored)
 *   Perpendicular dimensions use branded types
 */

/** meihua resolve_path — pure computation (no side effects) */
export function resolvePath(headedness: number): number {
  return (headedness === 'unheaded') ? 'conjunction' : (headedness === 'headed') ? 'filter' : 'expand';
}

/** meihua gate_check — pure computation (no side effects) */
export function gateCheck(role_count: number, max_roles: number): number {
  return ((role_count > max_roles)) ? 0 : 1;
}

