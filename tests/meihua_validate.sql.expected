-- Generated by suhc (the suihan compiler) from ./tests/meihua_validate.szh
-- DO NOT EDIT — regenerate from the .szh source.
--
-- Dependencies are emitted as CREATE TABLE statements.
-- Review and adjust column types before running.

-- meihua good_fn — pure computation
CREATE OR REPLACE FUNCTION good_fn(
  p_a double precision,
  p_b double precision
) RETURNS double precision
  LANGUAGE sql
  IMMUTABLE
AS $$
  SELECT (sqrt(p_a) + abs(p_b))
$$;

-- meihua bad_fn — pure computation
CREATE OR REPLACE FUNCTION bad_fn(
  p_x double precision,
  p_y double precision
) RETURNS double precision
  LANGUAGE sql
  IMMUTABLE
AS $$
  SELECT (undefined_thing(p_x) + mystery_call(p_y))
$$;

-- meihua ref_error — pure computation
CREATE OR REPLACE FUNCTION ref_error(
  p_p double precision,
  p_q double precision
) RETURNS double precision
  LANGUAGE sql
  IMMUTABLE
AS $$
  SELECT ((p_p + p_unknown_var) + p_q)
$$;

