/**
 * Generated by suhc (the suihan compiler) from ./tests/meihua_validate.szh
 * DO NOT EDIT — regenerate from the .szh source.
 *
 * Kind system guarantees:
 *   ξ values are immutable (const)
 *   ζ values are computed (functions, never stored)
 *   Perpendicular dimensions use branded types
 */

/** meihua good_fn — pure computation (no side effects) */
export function goodFn(a: number, b: number): number {
  return (Math.sqrt(a) + Math.abs(b));
}

/** meihua bad_fn — pure computation (no side effects) */
export function badFn(x: number, y: number): number {
  return (undefined_thing(x) + mystery_call(y));
}

/** meihua ref_error — pure computation (no side effects) */
export function refError(p: number, q: number): number {
  return ((p + unknown_var) + q);
}

