/**
 * Generated by suhc (the suihan compiler) from ./tests/kind_keyword_types.szh
 * DO NOT EDIT — regenerate from the .szh source.
 *
 * Kind system guarantees:
 *   ξ values are immutable (const)
 *   ζ values are computed (functions, never stored)
 *   Perpendicular dimensions use branded types
 */

/** unit meter */
export interface Meter {
  readonly id: number;
}

/** unit distance */
export interface Distance {
  readonly id: number;
}

/** unit velocity */
export interface Velocity {
  readonly id: number;
}

/** dimension directions */
type DirectionsValue = string | null;
export type Directions = DirectionsValue & { readonly __brand: 'directions' };

/** meihua typed_unit — pure computation (no side effects) */
export function typedUnit(a: number): number {
  return (a + 1);
}

/** meihua typed_magnitude — pure computation (no side effects) */
export function typedMagnitude(a: number, b: number): number {
  return (a + b);
}

/** meihua typed_vector — pure computation (no side effects) */
export function typedVector(a: number): number {
  return (a * 2);
}

/** meihua typed_dimension — pure computation (no side effects) */
export function typedDimension(a: number): number {
  return (a + 1);
}

