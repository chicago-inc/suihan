dimension test_roles : alpha | beta | gamma

projection complete_proj :
  invariant ξ test_role
  context ζ action
  yields ω result
  cases:
    (alpha, view) -> "a"
    (beta, view) -> "b"
    (gamma, view) -> "c"
    (_, _) -> "default"
