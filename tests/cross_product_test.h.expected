/*
 * Generated by suhc (the suihan compiler) from ./tests/cross_product_test.szh
 * DO NOT EDIT — regenerate from the .szh source.
 *
 * This is a self-contained C11 header.
 * Include it in exactly one translation unit, or use
 * static inline functions (already the default).
 */

#ifndef SUHC_GEN_CROSS_PRODUCT_TEST_SZH
#define SUHC_GEN_CROSS_PRODUCT_TEST_SZH

#include <math.h>
#include <stddef.h>
#include <stdbool.h>
#include <string.h>

/* dimension roles */
typedef enum {
    ROLES_OWNER,
    ROLES_ADMIN,
    ROLES_MEMBER
} roles_t;

static inline const char *roles_to_str(roles_t val) {
    switch (val) {
    case ROLES_OWNER: return "owner";
    case ROLES_ADMIN: return "admin";
    case ROLES_MEMBER: return "member";
    }
    return "unknown";
}

static inline roles_t roles_from_name(const char *name) {
    if (strcmp(name, "owner") == 0) return ROLES_OWNER;
    if (strcmp(name, "admin") == 0) return ROLES_ADMIN;
    if (strcmp(name, "member") == 0) return ROLES_MEMBER;
    return (roles_t)-1;
}

/* dimension actions */
typedef enum {
    ACTIONS_EDIT,
    ACTIONS_DELETE,
    ACTIONS_CANCEL
} actions_t;

static inline const char *actions_to_str(actions_t val) {
    switch (val) {
    case ACTIONS_EDIT: return "edit";
    case ACTIONS_DELETE: return "delete";
    case ACTIONS_CANCEL: return "cancel";
    }
    return "unknown";
}

static inline actions_t actions_from_name(const char *name) {
    if (strcmp(name, "edit") == 0) return ACTIONS_EDIT;
    if (strcmp(name, "delete") == 0) return ACTIONS_DELETE;
    if (strcmp(name, "cancel") == 0) return ACTIONS_CANCEL;
    return (actions_t)-1;
}

/* projection permission_matrix */
static inline const char *resolve_permission_matrix(
    const char *role,
    const char *action
) {
    if (strcmp(role, "owner") == 0 && strcmp(action, "edit") == 0) {
        return "true";
    } else if (strcmp(role, "owner") == 0 && strcmp(action, "delete") == 0) {
        return "true";
    } else if (strcmp(role, "admin") == 0 && strcmp(action, "edit") == 0) {
        return "true";
    } else if (strcmp(role, "admin") == 0 && strcmp(action, "delete") == 0) {
        return "false";
    } else if (strcmp(role, "member") == 0) {
        return "false";
    }
    return "denied";
}

#endif /* SUHC_GEN_CROSS_PRODUCT_TEST_SZH */
