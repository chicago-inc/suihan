/*
 * Generated by suhc (the suihan compiler) from ./tests/typed_simple.szh
 * DO NOT EDIT — regenerate from the .szh source.
 *
 * This is a self-contained C11 header.
 * Include it in exactly one translation unit, or use
 * static inline functions (already the default).
 */

#ifndef SUHC_GEN_TYPED_SIMPLE_SZH
#define SUHC_GEN_TYPED_SIMPLE_SZH

#include <math.h>
#include <stddef.h>
#include <stdbool.h>
#include <string.h>

/* unit meter */
typedef double meter_t;

/* unit kilogram */
typedef double kilogram_t;

/* magnitude distance */
typedef double distance_t;

/* magnitude weight */
typedef double weight_t;

/* meihua add_distance — pure computation */
static inline double add_distance(double a, double b) {
    return (a + b);
}

/* meihua scale — pure computation */
static inline double scale(double val, double factor) {
    return (val * factor);
}

#endif /* SUHC_GEN_TYPED_SIMPLE_SZH */
