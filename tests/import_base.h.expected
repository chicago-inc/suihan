/*
 * Generated by suhc (the suihan compiler) from ./tests/import_base.szh
 * DO NOT EDIT — regenerate from the .szh source.
 *
 * This is a self-contained C11 header.
 * Include it in exactly one translation unit, or use
 * static inline functions (already the default).
 */

#ifndef SUHC_GEN_IMPORT_BASE_SZH
#define SUHC_GEN_IMPORT_BASE_SZH

#include <math.h>
#include <stddef.h>
#include <stdbool.h>
#include <string.h>

/* unit user */
typedef double user_t;

/* unit resource */
typedef double resource_t;

/* dimension roles */
typedef enum {
    ROLES_OWNER,
    ROLES_ADMIN,
    ROLES_MEMBER,
    ROLES_PENDING
} roles_t;

static inline const char *roles_to_str(roles_t val) {
    switch (val) {
    case ROLES_OWNER: return "owner";
    case ROLES_ADMIN: return "admin";
    case ROLES_MEMBER: return "member";
    case ROLES_PENDING: return "pending";
    }
    return "unknown";
}

static inline roles_t roles_from_name(const char *name) {
    if (strcmp(name, "owner") == 0) return ROLES_OWNER;
    if (strcmp(name, "admin") == 0) return ROLES_ADMIN;
    if (strcmp(name, "member") == 0) return ROLES_MEMBER;
    if (strcmp(name, "pending") == 0) return ROLES_PENDING;
    return (roles_t)-1;
}

#endif /* SUHC_GEN_IMPORT_BASE_SZH */
