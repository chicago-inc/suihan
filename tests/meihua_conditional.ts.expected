/**
 * Generated by suhc (the suihan compiler) from ./tests/meihua_conditional.szh
 * DO NOT EDIT — regenerate from the .szh source.
 *
 * Kind system guarantees:
 *   ξ values are immutable (const)
 *   ζ values are computed (functions, never stored)
 *   Perpendicular dimensions use branded types
 */

/** meihua safe_divide — pure computation (no side effects) */
export function safeDivide(a: number, b: number): number {
  return ((b === 0)) ? 0 : (a / b);
}

/** meihua clamp_v2 — pure computation (no side effects) */
export function clampV2(value: number, low: number, high: number): number {
  return ((value < low)) ? low : ((value > high)) ? high : value;
}

/** meihua abs_value — pure computation (no side effects) */
export function absValue(x: number): number {
  return ((x < 0)) ? (-x) : x;
}

/** meihua sign — pure computation (no side effects) */
export function sign(n: number): number {
  return ((n > 0)) ? 1 : ((n < 0)) ? (-1) : 0;
}


/* Kind manifest — 256-bit perpendicular encoding metadata */
export const __kindManifest = {
  'safe_divide': 'xi',
  'clamp_v2': 'xi',
  'abs_value': 'xi',
  'sign': 'xi'
} as const;

export type KindOf<K extends keyof typeof __kindManifest> = typeof __kindManifest[K];
