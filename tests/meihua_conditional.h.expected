/*
 * Generated by suhc (the suihan compiler) from ./tests/meihua_conditional.szh
 * DO NOT EDIT — regenerate from the .szh source.
 *
 * This is a self-contained C11 header.
 * Include it in exactly one translation unit, or use
 * static inline functions (already the default).
 */

#ifndef SUHC_GEN_MEIHUA_CONDITIONAL_SZH
#define SUHC_GEN_MEIHUA_CONDITIONAL_SZH

#include <math.h>
#include <stddef.h>
#include <stdbool.h>
#include <string.h>

/* meihua safe_divide — pure computation */
static inline double safe_divide(double a, double b) {
    return ((b == 0)) ? 0 : (a / b);
}

/* meihua clamp_v2 — pure computation */
static inline double clamp_v2(double value, double low, double high) {
    return ((value < low)) ? low : ((value > high)) ? high : value;
}

/* meihua abs_value — pure computation */
static inline double abs_value(double x) {
    return ((x < 0)) ? (-x) : x;
}

/* meihua sign — pure computation */
static inline double sign(double n) {
    return ((n > 0)) ? 1 : ((n < 0)) ? (-1) : 0;
}

#endif /* SUHC_GEN_MEIHUA_CONDITIONAL_SZH */
