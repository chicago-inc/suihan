dimension test_roles : alpha | beta | gamma

projection incomplete_proj :
  invariant ξ test_role
  context ζ action
  yields ω result
  cases:
    (alpha, view) -> "yes"
    (beta, view) -> "yes"
    (_, _) -> "no"
