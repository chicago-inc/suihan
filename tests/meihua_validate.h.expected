/*
 * Generated by suhc (the suihan compiler) from ./tests/meihua_validate.szh
 * DO NOT EDIT — regenerate from the .szh source.
 *
 * This is a self-contained C11 header.
 * Include it in exactly one translation unit, or use
 * static inline functions (already the default).
 */

#ifndef SUHC_GEN_MEIHUA_VALIDATE_SZH
#define SUHC_GEN_MEIHUA_VALIDATE_SZH

#include <math.h>
#include <stddef.h>
#include <stdbool.h>
#include <string.h>

/* meihua good_fn — pure computation */
static inline double good_fn(double a, double b) {
    return (sqrt(a) + fabs(b));
}

/* meihua bad_fn — pure computation */
static inline double bad_fn(double x, double y) {
    return (undefined_thing(x) + mystery_call(y));
}

/* meihua ref_error — pure computation */
static inline double ref_error(double p, double q) {
    return ((p + unknown_var) + q);
}

#endif /* SUHC_GEN_MEIHUA_VALIDATE_SZH */
