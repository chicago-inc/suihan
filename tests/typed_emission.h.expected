/*
 * Generated by suhc (the suihan compiler) from ./tests/typed_emission.szh
 * DO NOT EDIT — regenerate from the .szh source.
 *
 * This is a self-contained C11 header.
 * Include it in exactly one translation unit, or use
 * static inline functions (already the default).
 */

#ifndef SUHC_GEN_TYPED_EMISSION_SZH
#define SUHC_GEN_TYPED_EMISSION_SZH

#include <math.h>
#include <stddef.h>
#include <stdbool.h>
#include <string.h>

/* dimension colors */
typedef enum {
    COLORS_RED,
    COLORS_GREEN,
    COLORS_BLUE
} colors_t;

static inline const char *colors_to_str(colors_t val) {
    switch (val) {
    case COLORS_RED: return "red";
    case COLORS_GREEN: return "green";
    case COLORS_BLUE: return "blue";
    }
    return "unknown";
}

static inline colors_t colors_from_name(const char *name) {
    if (strcmp(name, "red") == 0) return COLORS_RED;
    if (strcmp(name, "green") == 0) return COLORS_GREEN;
    if (strcmp(name, "blue") == 0) return COLORS_BLUE;
    return (colors_t)-1;
}

/* dimension sizes */
typedef enum {
    SIZES_SMALL,
    SIZES_MEDIUM,
    SIZES_LARGE
} sizes_t;

static inline const char *sizes_to_str(sizes_t val) {
    switch (val) {
    case SIZES_SMALL: return "small";
    case SIZES_MEDIUM: return "medium";
    case SIZES_LARGE: return "large";
    }
    return "unknown";
}

static inline sizes_t sizes_from_name(const char *name) {
    if (strcmp(name, "small") == 0) return SIZES_SMALL;
    if (strcmp(name, "medium") == 0) return SIZES_MEDIUM;
    if (strcmp(name, "large") == 0) return SIZES_LARGE;
    return (sizes_t)-1;
}

/* projection color_label */
static inline const char *resolve_color_label(
    const char *color,
    const char *size
) {
    if (strcmp(color, "red") == 0 && strcmp(size, "small") == 0) {
        return "Small Red";
    } else if (strcmp(color, "red") == 0 && strcmp(size, "medium") == 0) {
        return "Medium Red";
    } else if (strcmp(color, "red") == 0 && strcmp(size, "large") == 0) {
        return "Large Red";
    } else if (strcmp(color, "green") == 0 && strcmp(size, "small") == 0) {
        return "Small Green";
    } else if (strcmp(color, "green") == 0 && strcmp(size, "medium") == 0) {
        return "Medium Green";
    } else if (strcmp(color, "green") == 0 && strcmp(size, "large") == 0) {
        return "Large Green";
    } else if (strcmp(color, "blue") == 0 && strcmp(size, "small") == 0) {
        return "Small Blue";
    } else if (strcmp(color, "blue") == 0 && strcmp(size, "medium") == 0) {
        return "Medium Blue";
    } else if (strcmp(color, "blue") == 0 && strcmp(size, "large") == 0) {
        return "Large Blue";
    }
    return "Unknown";
}

#endif /* SUHC_GEN_TYPED_EMISSION_SZH */
