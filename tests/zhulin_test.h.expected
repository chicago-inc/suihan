/*
 * Generated by suhc (the suihan compiler) from ./tests/zhulin_test.szh
 * DO NOT EDIT — regenerate from the .szh source.
 *
 * This is a self-contained C11 header.
 * Include it in exactly one translation unit, or use
 * static inline functions (already the default).
 */

#ifndef SUHC_GEN_ZHULIN_TEST_SZH
#define SUHC_GEN_ZHULIN_TEST_SZH

#include <math.h>
#include <stddef.h>
#include <stdbool.h>
#include <string.h>

/* zhulin resolve_path — control flow */
static inline const char *resolve_path(const char *headedness) {
    return (strcmp(headedness, "unheaded") == 0 ? "conjunction" : (strcmp(headedness, "headed") == 0 ? "filter" : "expand"));
}

/* zhulin gate_check — control flow */
static inline doublegate_check(doublerole_count, doublemax_roles) {
    return ((role_count > max_roles)) ? 0 : 1;
}

#endif /* SUHC_GEN_ZHULIN_TEST_SZH */
